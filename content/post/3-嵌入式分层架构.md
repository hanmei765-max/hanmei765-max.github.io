---
title: "嵌入式分层架构"
date: 2026-04-25
categories: ["嵌入式"]
tags: ["系统","架构"]
author: "hanmei765-max"
---


## 智能门锁项目文件结构示例



## 一、整体项目结构

```
SmartLock/
├── app/                          # 应用层
│   ├── door_logic.c
│   ├── key_manager.c
│   └── alarm_system.c
│
├── middleware/                   # 中间件/接口层
│   ├── api_gateway.c
│   └── command_processor.c
│
├── driver/                       # 设备驱动层（底层）
│   ├── gpio/
│   │   ├── drv_gpio.c
│   │   └── drv_gpio.h
│   ├── uart/
│   │   ├── drv_uart.c
│   │   └── drv_uart.h
│   ├── spi/
│   │   ├── drv_spi.c
│   │   └── drv_spi.h
│   ├── i2c/
│   │   ├── drv_i2c.c
│   │   └── drv_i2c.h
│   ├── adc/
│   │   ├── drv_adc.c
│   │   └── drv_adc.h
│   └── fspi/                    # FSMC/FMC Flash驱动
│       ├── drv_fmc.c
│       └── drv_fmc.h
│
├── bsp/                          # 板级支持包
│   ├── bsp_config.h             # 核心配置文件
│   ├── bsp_pins.c
│   ├── bsp_pins.h
│   ├── bsp_init.c
│   ├── bsp_init.h
│   ├── clock.c
│   ├── clock.h
│   ├── led/
│   │   ├── bsp_led.c
│   │   └── bsp_led.h
│   ├── keypad/
│   │   ├── bsp_keypad.c
│   │   └── bsp_keypad.h
│   └── screen/
│       ├── bsp_screen.c
│       └── bsp_screen.h
│
├── hal/                          # 硬件抽象层
│   ├── gpio/
│   │   ├── hal_gpio.h
│   │   └── hal_gpio.c
│   ├── uart/
│   │   ├── hal_uart.h
│   │   └── hal_uart.c
│   ├── spi/
│   │   ├── hal_spi.h
│   │   └── hal_spi.c
│   ├── i2c/
│   │   ├── hal_i2c.h
│   │   └── hal_i2c.c
│   ├── delay/
│   │   ├── hal_delay.h
│   │   └── hal_delay.c
│   └── sys/
│       ├── hal_sys.h
│       └── hal_sys.c
│
├── hw/                           # 硬件定义层
│   ├── stm32h7xxx.h
│   ├── system_stm32h7xx.c
│   └── startup_stm32h7xx.s
│
├── project.svd                   # 芯片描述文件
├── stm32h7xx.svd
└── README.md
```




## 二、驱动层文件结构详解

### 1️⃣ GPIO 驱动层

```c
// driver/gpio/drv_gpio.h
#ifndef DRV_GPIO_H
#define DRV_GPIO_H

#include <stdint.h>

// 定义GPIO端口类型（与具体MCU对齐）
typedef enum {
    DRV_GPIO_PORT_A,
    DRV_GPIO_PORT_B,
    DRV_GPIO_PORT_C,
    DRV_GPIO_PORT_D,
    DRV_GPIO_PORT_E,
} DRV_GPIO_Port_t;

// 定义GPIO方向
typedef enum {
    DRV_GPIO_MODE_INPUT,
    DRV_GPIO_MODE_OUTPUT,
    DRV_GPIO_MODE_ALTERNATE,
    DRV_GPIO_MODE_ANALOG
} DRV_GPIO_Mode_t;

// 定义GPIO输出速度
typedef enum {
    DRV_GPIO_SPEED_LOW = 0,
    DRV_GPIO_SPEED_MEDIUM,
    DRV_GPIO_SPEED_HIGH,
    DRV_GPIO_SPEED_VERY_HIGH
} DRV_GPIO_Speed_t;

// 定义GPIO上拉/下拉
typedef enum {
    DRV_GPIO_NOPULL = 0,
    DRV_GPIO_PULLUP,
    DRV_GPIO_PULLDOWN
} DRV_GPIO_Pull_t;

// 底层驱动函数声明
void DRV_GPIO_Init(DRV_GPIO_Port_t port, uint32_t pin, DRV_GPIO_Mode_t mode, 
                   DRV_GPIO_Speed_t speed, DRV_GPIO_Pull_t pull);

void DRV_GPIO_Write(DRV_GPIO_Port_t port, uint32_t pin, uint8_t value);

uint8_t DRV_GPIO_Read(DRV_GPIO_Port_t port, uint32_t pin);

void DRV_GPIO_Toggle(DRV_GPIO_Port_t port, uint32_t pin);

void DRV_GPIO_EnableClock(DRV_GPIO_Port_t port);

#endif
```



```c
// driver/gpio/drv_gpio.c
#include "drv_gpio.h"
#include <stdint.h>

// ========== 寄存器映射 ==========
// STM32H7寄存器基地址定义
#define GPIOA_BASE    0x50020000
#define GPIOB_BASE    0x50020400
#define GPIOC_BASE    0x50020800

typedef struct {
    volatile uint32_t MODER;      // 0x00 - 模式寄存器
    volatile uint32_t OTYPER;     // 0x04 - 输出类型寄存器
    volatile uint32_t OSPEEDR;    // 0x08 - 输出速度寄存器
    volatile uint32_t PUPDR;      // 0x0C - 上拉/下拉寄存器
    volatile uint32_t IDR;        // 0x10 - 输入数据寄存器
    volatile uint32_t ODR;        // 0x14 - 输出数据寄存器
    volatile uint32_t BSRR;       // 0x18 - 置位/复位寄存器
    volatile uint32_t LCKR;       // 0x1C - 配置锁定寄存器
    volatile uint32_t AFR[2];     // 0x20/0x24 -  Alternate Function
} GPIO_Regs_t;

// ========== 寄存器基地址定义 ==========
#define GPIOA_REG   ((GPIO_Regs_t *)GPIOA_BASE)
#define GPIOB_REG   ((GPIO_Regs_t *)GPIOB_BASE)
#define GPIOC_REG   ((GPIO_Regs_t *)GPIOC_BASE)

// ========== 底层实现 ==========

// GPIO时钟使能（直接操作RCC寄存器）
void DRV_GPIO_EnableClock(DRV_GPIO_Port_t port) {
    // STM32H7 RCC_BASE = 0x40021000
    volatile uint32_t *rcc_cr = (uint32_t *)0x40021028;  // RCC_AHB1ENR
    
    uint32_t clock_mask = 0x0;
    switch(port) {
        case DRV_GPIO_PORT_A: clock_mask = (1 << 0);  break;
        case DRV_GPIO_PORT_B: clock_mask = (1 << 1);  break;
        case DRV_GPIO_PORT_C: clock_mask = (1 << 2);  break;
        case DRV_GPIO_PORT_D: clock_mask = (1 << 3);  break;
        case DRV_GPIO_PORT_E: clock_mask = (1 << 4);  break;
    }
    *rcc_cr |= clock_mask;
}

// GPIO初始化（驱动层最底层操作）
void DRV_GPIO_Init(DRV_GPIO_Port_t port, uint32_t pin, DRV_GPIO_Mode_t mode, 
                   DRV_GPIO_Speed_t speed, DRV_GPIO_Pull_t pull) {
    GPIO_Regs_t *gpio = NULL;
    switch(port) {
        case DRV_GPIO_PORT_A: gpio = GPIOA_REG;  break;
        case DRV_GPIO_PORT_B: gpio = GPIOB_REG;  break;
        case DRV_GPIO_PORT_C: gpio = GPIOC_REG;  break;
        default: return;
    }
    
    // 计算引脚偏移
    uint32_t pin_idx = pin / 16;
    uint32_t pin_shift = (pin % 16) * 2;
    
    // 配置模式寄存器 (MODER)
    gpio->MODER &= ~(0x3 << pin_shift);
    gpio->MODER |= (mode << pin_shift);
    
    // 配置输出速度 (OSPEEDR)
    gpio->OSPEEDR &= ~(0x3 << pin_shift);
    gpio->OSPEEDR |= (speed << pin_shift);
    
    // 配置上拉/下拉 (PUPDR)
    gpio->PUPDR &= ~(0x3 << pin_shift);
    gpio->PUPDR |= (pull << pin_shift);
}

// GPIO写操作（直接操作ODR寄存器）
void DRV_GPIO_Write(DRV_GPIO_Port_t port, uint32_t pin, uint8_t value) {
    GPIO_Regs_t *gpio = NULL;
    switch(port) {
        case DRV_GPIO_PORT_A: gpio = GPIOA_REG;  break;
        case DRV_GPIO_PORT_B: gpio = GPIOB_REG;  break;
        default: return;
    }
    
    if(value) {
        gpio->BSRR = (1 << pin);  // 置位
    } else {
        gpio->BSRR = (1 << (pin + 16));  // 复位
    }
}

// GPIO读操作（直接读IDR寄存器）
uint8_t DRV_GPIO_Read(DRV_GPIO_Port_t port, uint32_t pin) {
    GPIO_Regs_t *gpio = NULL;
    switch(port) {
        case DRV_GPIO_PORT_A: gpio = GPIOA_REG;  break;
        default: return 0;
    }
    return (gpio->IDR >> pin) & 0x1;
}

// GPIO翻转（直接写BSRR寄存器）
void DRV_GPIO_Toggle(DRV_GPIO_Port_t port, uint32_t pin) {
    GPIO_Regs_t *gpio = NULL;
    switch(port) {
        case DRV_GPIO_PORT_A: gpio = GPIOA_REG;  break;
        default: return;
    }
    gpio->BSRR = (1 << pin);  // 翻转操作
}
```





## 三、BSP层文件结构详解

### 2️⃣ BSP 配置层

```c
// bsp/bsp_config.h
#ifndef BSP_CONFIG_H
#define BSP_CONFIG_H

// ========== 系统时钟配置 ==========
#define BSP_SYS_HCLK        400000000UL   // 系统时钟 400MHz
#define BSP_SYS_MCLK        400000000UL   // 外设时钟
#define BSP_SYS_PLLSRC      HSE          // 时钟源 HSE
#define BSP_SYS_PLLM        8
#define BSP_SYS_PLLN        400
#define BSP_SYS_PLLP        2
#define BSP_SYS_QDIV        4
#define BSP_SYS_PDIV        2

// ========== UART 配置 ==========
#define BSP_UART1_RX_PIN    GPIO_PIN_10   // UART1_RX on PC10
#define BSP_UART1_TX_PIN    GPIO_PIN_9    // UART1_TX on PC9
#define BSP_UART1_BAUD      115200

#define BSP_UART2_RX_PIN    GPIO_PIN_0    // UART2_RX on PA0
#define BSP_UART2_TX_PIN    GPIO_PIN_1    // UART2_TX on PA1
#define BSP_UART2_BAUD      115200

// ========== I2C 配置 ==========
#define BSP_I2C1_SCL_PIN    GPIO_PIN_6    // I2C1_SCL on PB6
#define BSP_I2C1_SDA_PIN    GPIO_PIN_7    // I2C1_SDA on PB7
#define BSP_I2C1_SPEED      400000UL      // 400kHz

// ========== SPI 配置 ==========
#define BSP_SPI1_CS_PIN     GPIO_PIN_5    // SPI1_CS on PA5
#define BSP_SPI1_SCK_PIN    GPIO_PIN_5    // SPI1_SCK on PA5
#define BSP_SPI1_MOSI_PIN   GPIO_PIN_7    // SPI1_MOSI on PA7
#define BSP_SPI1_MISO_PIN   GPIO_PIN_6    // SPI1_MISO on PA6

// ========== Flash 存储配置 ==========
#define BSP_FLASH_START_ADDR    0x08000000  // Flash起始地址
#define BSP_FLASH_SIZE          0x100000    // 1MB Flash
#define BSP_EEPROM_ADDR         0x08100000  // EEPROM仿真地址

// ========== 门锁特有硬件配置 ==========
// 指纹模组
#define BSP_FINGERPRINT_I2C         I2C1
#define BSP_FINGERPRINT_SCL_PIN     GPIO_PIN_6
#define BSP_FINGERPRINT_SDA_PIN     GPIO_PIN_7
#define BSP_FINGERPRINT_RESET_PIN   GPIO_PIN_15

// WiFi模块
#define BSP_WIFI_UART               UART2
#define BSP_WIFI_RESET_PIN          GPIO_PIN_3
#define BSP_WIFI_PWR_PIN            GPIO_PIN_4

// 屏幕
#define BSP_LCD_CS_PIN              GPIO_PIN_12
#define BSP_LCD_DC_PIN              GPIO_PIN_13
#define BSP_LCD_RST_PIN             GPIO_PIN_14
#define BSP_LCD_WIDTH               240
#define BSP_LCD_HEIGHT              320

// LED状态指示
#define BSP_LED_OK_PIN              GPIO_PIN_0
#define BSP_LED_ERR_PIN             GPIO_PIN_1
#define BSP_LED_CHARGING_PIN        GPIO_PIN_2

// 按键
#define BSP_KEY_LOCK_PIN            GPIO_PIN_10
#define BSP_KEY_UNLOCK_PIN          GPIO_PIN_11
#define BSP_KEY_ADMIN_PIN           GPIO_PIN_12
#define BSP_KEY_WAKE_PIN            GPIO_PIN_13

// RTC
#define BSP_RTC_CLK                 LSE
#define BSP_RTC_ALARM_TIME          0  // 闹钟时间

// ========== 硬件检测宏 ==========
#define BSP_HAS_FINGERPRINT         1
#define BSP_HAS_WIFI                1
#define BSP_HAS_BT                  1
#define BSP_HAS_LCD                 1
#define BSP_HAS_KEYPAD              1
#define BSP_HAS_LOCK_MOTOR          1

#endif
```



```c
// bsp/bsp_init.c
#include "bsp_config.h"
#include "drv_gpio.h"
#include "drv_uart.h"
#include "drv_spi.h"
#include "hal_delay.h"
#include <stdint.h>

// ========== 板级初始化函数 ==========

// 系统时钟配置
void BSP_Clock_Init(void) {
    // 配置PLL（调用底层RCC寄存器）
    // 配置HSE使能
    // 配置PLL参数
    // 切换系统时钟源
}

// GPIO初始化
void BSP_GPIO_Init(void) {
    // 使能所有GPIO时钟
    DRV_GPIO_EnableClock(DRV_GPIO_PORT_A);
    DRV_GPIO_EnableClock(DRV_GPIO_PORT_B);
    DRV_GPIO_EnableClock(DRV_GPIO_PORT_C);
    
    // 初始化所有GPIO引脚
    DRV_GPIO_Init(DRV_GPIO_PORT_A, BSP_UART1_TX_PIN, 
                  DRV_GPIO_MODE_ALTERNATE, DRV_GPIO_SPEED_HIGH, 
                  DRV_GPIO_NOPULL);
    
    DRV_GPIO_Init(DRV_GPIO_PORT_A, BSP_UART1_RX_PIN,
                  DRV_GPIO_MODE_INPUT, DRV_GPIO_SPEED_LOW,
                  DRV_GPIO_PULLUP);
    
    // ... 更多引脚配置
}

// UART初始化
void BSP_UART_Init(void) {
    // UART1配置
    DRV_UART_Init(UART1, BSP_UART1_BAUD);
    DRV_UART_SetBaud(UART1, BSP_UART1_BAUD);
    
    // UART2配置
    DRV_UART_Init(UART2, BSP_UART2_BAUD);
}

// I2C初始化
void BSP_I2C_Init(void) {
    DRV_I2C_Init(I2C1, BSP_I2C1_SPEED);
}

// 系统级初始化
void BSP_System_Init(void) {
    // 1. 时钟配置
    BSP_Clock_Init();
    
    // 2. GPIO配置
    BSP_GPIO_Init();
    
    // 3. UART配置
    BSP_UART_Init();
    
    // 4. 其他外设配置
    BSP_I2C_Init();
    
    // 5. 中断向量表配置
    NVIC_SetVectorTable(NVIC_VectTab_FLASH, 0x0);
}
```



```c
// bsp/bsp_pins.h
#ifndef BSP_PINS_H
#define BSP_PINS_H

#include "bsp_config.h"

// ========== 门锁LED引脚定义 ==========
typedef enum {
    BSP_PIN_LED_OK      = 0,
    BSP_PIN_LED_ERR,
    BSP_PIN_LED_CHARGING
} BSP_LED_Pin_t;

// ========== 门锁按键引脚定义 ==========
typedef enum {
    BSP_PIN_KEY_LOCK    = 0,
    BSP_PIN_KEY_UNLOCK,
    BSP_PIN_KEY_ADMIN,
    BSP_PIN_KEY_WAKE
} BSP_Key_Pin_t;

// ========== 指纹模组引脚定义 ==========
typedef enum {
    BSP_PIN_FP_SCLK     = 0,
    BSP_PIN_FP_SDIO,
    BSP_PIN_FP_RST,
    BSP_PIN_FP_IRQ
} BSP_FP_Pin_t;

// ========== WiFi模块引脚定义 ==========
typedef enum {
    BSP_PIN_WIFI_RST    = 0,
    BSP_PIN_WIFI_PWR,
    BSP_PIN_WIFI_CS
} BSP_WIFI_Pin_t;

// ========== 屏幕引脚定义 ==========
typedef enum {
    BSP_PIN_LCD_CS      = 0,
    BSP_PIN_LCD_DC,
    BSP_PIN_LCD_RST,
    BSP_PIN_LCD_SCK,
    BSP_PIN_LCD_MOSI
} BSP_LCD_Pin_t;

#endif
```





## 四、HAL层文件结构详解

### 3️⃣ HAL GPIO层

```c
// hal/gpio/hal_gpio.h
#ifndef HAL_GPIO_H
#define HAL_GPIO_H

#include "stdint.h"

// HAL层GPIO状态定义
typedef enum {
    HAL_GPIO_LEVEL_LOW  = 0,
    HAL_GPIO_LEVEL_HIGH
} HAL_GPIO_Level_t;

// HAL层GPIO初始化结构
typedef struct {
    uint8_t  port;       // GPIO端口 A,B,C,D,E
    uint32_t pin;        // 引脚号 0-15
    uint8_t  mode;       // 输入/输出/复用/模拟
    uint8_t  speed;      // 输出速度
    uint8_t  pull;       // 上拉/下拉
} HAL_GPIO_Init_t;

// ========== HAL GPIO 接口定义 ==========

// GPIO初始化
void HAL_GPIO_Init(GPIO_Init_t *init);

// 引脚写操作
void HAL_GPIO_WritePin(GPIO_TypeDef *port, uint16_t pin, uint8_t level);

// 引脚读操作
uint8_t HAL_GPIO_ReadPin(GPIO_TypeDef *port, uint16_t pin);

// 引脚翻转
void HAL_GPIO_TogglePin(GPIO_TypeDef *port, uint16_t pin);

// 引脚配置
void HAL_GPIO_ConfigPin(uint8_t port, uint16_t pin, uint8_t mode);

#endif
```



```c
// hal/gpio/hal_gpio.c
#include "hal_gpio.h"
#include "bsp_config.h"
#include "drv_gpio.h"

// HAL GPIO初始化
void HAL_GPIO_Init(HAL_GPIO_Init_t *init) {
    DRV_GPIO_Port_t port = DRV_GPIO_PORT_A + init->port;
    DRV_GPIO_Mode_t mode;
    DRV_GPIO_Speed_t speed = DRV_GPIO_SPEED_HIGH;
    DRV_GPIO_Pull_t pull = DRV_GPIO_NOPULL;
    
    // 模式转换
    switch(init->mode) {
        case HAL_GPIO_MODE_INPUT:       mode = DRV_GPIO_MODE_INPUT;       break;
        case HAL_GPIO_MODE_OUTPUT:      mode = DRV_GPIO_MODE_OUTPUT;      break;
        case HAL_GPIO_MODE_ALTERNATE:   mode = DRV_GPIO_MODE_ALTERNATE;   break;
        case HAL_GPIO_MODE_ANALOG:      mode = DRV_GPIO_MODE_ANALOG;      break;
        default:                        mode = DRV_GPIO_MODE_INPUT;       break;
    }
    
    // 调用驱动层接口
    DRV_GPIO_Init(port, init->pin, mode, speed, pull);
}

// HAL GPIO写操作
void HAL_GPIO_WritePin(GPIO_TypeDef *port, uint16_t pin, uint8_t level) {
    DRV_GPIO_Port_t drv_port;
    uint32_t drv_pin = pin;
    
    // 端口号映射
    if(port == GPIOA) drv_port = DRV_GPIO_PORT_A;
    else if(port == GPIOB) drv_port = DRV_GPIO_PORT_B;
    else if(port == GPIOC) drv_port = DRV_GPIO_PORT_C;
    else return;
    
    DRV_GPIO_Write(drv_port, drv_pin, level);
}

// HAL GPIO读操作
uint8_t HAL_GPIO_ReadPin(GPIO_TypeDef *port, uint16_t pin) {
    DRV_GPIO_Port_t drv_port;
    uint32_t drv_pin = pin;
    
    if(port == GPIOA) drv_port = DRV_GPIO_PORT_A;
    else if(port == GPIOB) drv_port = DRV_GPIO_PORT_B;
    else if(port == GPIOC) drv_port = DRV_GPIO_PORT_C;
    else return 0;
    
    return DRV_GPIO_Read(drv_port, drv_pin);
}
```





## 五、各层文件调用关系

```
智能门锁应用层代码:
app/door_logic.c

┌──────────────────────────────────────────────────┐
│  HAL_GPIO_WritePin(GPIOC, BSP_KEY_LOCK_PIN,     │ ← 应用层调用HAL接口
│                     HAL_GPIO_LEVEL_HIGH);       │
│                                                  │
│  HAL_UART_Transmit(&huart1, data, len, timeout);│
└──────────────────────────────────────────────────┘
                         ↓
┌──────────────────────────────────────────────────┐
│  hal/gpio/hal_gpio.c                             │
│  - 统一GPIO操作接口                               │
│  - 转换参数到驱动层                               │
└──────────────────────────────────────────────────┘
                         ↓
┌──────────────────────────────────────────────────┐
│  bsp/bsp_pins.h                                  │
│  - BSP定义BSP_KEY_LOCK_PIN                       │
│  - 引脚映射表                                     │
└──────────────────────────────────────────────────┘
                         ↓
┌──────────────────────────────────────────────────┐
│  driver/gpio/drv_gpio.c                          │
│  - 直接操作GPIOA/B/C寄存器                       │
│  - 最底层硬件操作                                 │
└──────────────────────────────────────────────────┘
                         ↓
┌──────────────────────────────────────────────────┐
│  STM32H7 Physical Hardware                       │
│  - GPIO寄存器物理访问                             │
│  - 电平输出                                       │
└──────────────────────────────────────────────────┘
```





## 六、完整调用示例

```c
// 应用层：智能门锁开门逻辑
// app/door_logic.c

#include "hal_gpio.h"
#include "hal_uart.h"
#include "bsp_config.h"
#include "hal_delay.h"

// 门锁状态：0=锁定，1=解锁
uint8_t door_status = 0;

// 开门函数
void Door_Unlock(void) {
    // 使用HAL层接口（应用层不关心底层硬件）
    HAL_GPIO_WritePin(GPIOC, BSP_KEY_UNLOCK_PIN, HAL_GPIO_LEVEL_HIGH);
    
    // 延时等待解锁
    HAL_Delay(500);
    
    // 发送开门成功消息
    uint8_t msg[] = "DOOR_UNLOCK_SUCCESS\r\n";
    HAL_UART_Transmit(&huart1, msg, sizeof(msg) - 1, 100);
    
    // 设置门锁状态
    door_status = 1;
    
    // 关闭开门信号
    HAL_GPIO_WritePin(GPIOC, BSP_KEY_UNLOCK_PIN, HAL_GPIO_LEVEL_LOW);
}

// 门锁检测
void Door_Check(void) {
    uint8_t key_state = HAL_GPIO_ReadPin(GPIOC, BSP_KEY_LOCK_PIN);
    if(key_state == HAL_GPIO_LEVEL_HIGH) {
        Door_Unlock();
    }
}
```





## 七、各层文件功能对比

| 层级 | 文件类型 | 典型文件名 | 职责 | 稳定性 |
|------|---------|-----------|------|--------|
| **驱动层** | C/H | `drv_gpio.c`, `drv_uart.h` | 直接操作硬件寄存器 | 低（依赖具体MCU） |
| **BSP层** | C/H | `bsp_config.h`, `bsp_init.c` | 板卡特定配置 | 中（依赖具体板卡） |
| **HAL层** | C/H | `hal_gpio.c`, `hal_uart.h` | 统一抽象接口 | 高（向上层稳定） |
| **应用层** | C/H | `door_logic.c`, `key_manager.c` | 业务逻辑 | 最高 |

---

## 八、更换MCU时的影响范围

| 需要修改的部分 | 影响层级 | 修改量 |
|---------------|---------|--------|
| 更换STM32型号 | 驱动层 | 修改寄存器定义 |
| 更换开发板 | BSP层 | 修改引脚映射 |
| 更换MCU但引脚相同 | HAL层 | 只需重编译 |
| 应用层代码 | 应用层 | **无需修改** |

这种分层设计使得**应用层代码可以完全复用**，只需修改底层三层即可适配新硬件。
